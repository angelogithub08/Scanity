import { api } from 'src/boot/axios';
import type { {{pascalCase singular}} } from 'src/interfaces/{{kebabCase module}}';
import { use{{pascalCase module}}Store } from 'src/stores/{{kebabCase module}}';

export function use{{pascalCase module}}Resource() {
  const { set{{pascalCase module}} } = use{{pascalCase module}}Store();
  
  function findAll(params = {}) {
    return api.get('/{{kebabCase module}}', { params });
  }

  function findById(id: string) {
    return api.get(`/{{kebabCase module}}/${id}`);
  }

  function list(params = {}) {
    return api.get('/{{kebabCase module}}/list', { params });
  }

  function create(data: Partial<{{pascalCase singular}}>) {
    return api.post('/{{kebabCase module}}', data);
  }

  function update(id: string, data: Partial<{{pascalCase singular}}>) {
    return api.put(`/{{kebabCase module}}/${id}`, data);
  }

  function destroy(id: string) {
    return api.delete(`/{{kebabCase module}}/${id}`);
  }

  async function load{{pascalCase module}}(params = {}) {
    const { data } = await api.get('/{{kebabCase module}}/list', { params });
    set{{pascalCase module}}(data);
  }

  return { findAll, findById, list, create, update, destroy, load{{pascalCase module}} };
}
